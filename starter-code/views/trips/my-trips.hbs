<h1>
  Look at all my trips!
</h1>

<ul>
  {{#each user.trips}}
  <li>
    <img src="/images/ironhack.png" alt="" class="activity-img">
    <h2 class="activity-text activity-title">{{this.name}}</h2>
    <p class="activity-text activity-location">{{this.location}}</p>
    <p>Budget: {{this.budget}} €</p>
    <a href="/{{this._id}}/addActivity">Add activities</a>
    <a href="/trips/{{this._id}}/edit">Edit</a>
    <form action="/trips/{{this._id}}/delete" method="post">
      <div>
        <button type="submit">Delete</button>
      </div>
    </form>
    {{!-- <ul>
      {{#each activities}}
      <li>
        <h2>{{this.name}}</h2>
        <p>{{this.location}}</p>
      </li>
      {{/each}}
    </ul> --}}
  </li>
  {{/each}}
</ul>

<p><a href="/trips/create">Create Trip</a></p>